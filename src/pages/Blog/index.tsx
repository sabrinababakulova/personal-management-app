import React from "react";

const Blog = () => {
  return (
    <div>
      The blog does not exist, the database is not connected, and everything is
      stored in local storage, which is not ideal
    </div>
  );
};

export default Blog;
